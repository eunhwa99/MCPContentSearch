# Runtime compatibility mirror for pip-only environments.
#
# Source of truth:
# - pyproject.toml declares runtime and dev dependencies.
# - uv.lock records the resolved dependency graph used by local verification and CI.
#
# CI does not install from this file. Keep this mirror runtime-only and update it
# only when pip-based consumers need a non-uv fallback.

# MCP
mcp[cli]>=1.19.0

# Server & Dependencies
fastapi>=0.136.3
uvicorn[standard]>=0.38.0
httpx>=0.27.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
certifi>=2024.2.2
python-dotenv>=1.0.0
pydantic>=2.6.0
tenacity>=8.2.0

# LlamaIndex (keep packages on the same modern line)
llama-index>=0.14.22
llama-index-core>=0.14.22
llama-index-vector-stores-chroma>=0.5.3
openai>=2.6.1

# Vector Store (Chroma)
chromadb>=1.2.1

# Others
numpy>=1.26.0
