interface:
  display_name: "Harness Review"
  short_description: "$subagent-review-loop gate for bugs and contracts"
  default_prompt: "$harness-review를 사용해 중간 또는 최종 review gate에서 $subagent-review-loop를 실행하고 MCP contract, Chroma/vector data, SQLite lifecycle/tombstone metadata, external connectors, secrets, async, tests를 검토하세요."
