"""Compatibility imports for callers that still use the former indexing path."""

from core.error_sanitizer import safe_error_message, sanitize_error_text

__all__ = ["safe_error_message", "sanitize_error_text"]
